---
phase: 05-design-foundation
plan: 02
type: execute
wave: 2
depends_on: [05-01]
files_modified:
  - components/dashboard/TransactionsPreviewTable.tsx
  - components/dashboard/ParentBudgetOverview.tsx
  - components/dashboard/TransparencyCard.tsx
autonomous: false
gap_closure: true

must_haves:
  truths:
    - 'All CardTitle elements across dashboard render at 18px (text-lg)'
    - 'No CardTitle overrides with text-base remain in dashboard components'
    - 'Visual hierarchy is consistent across all dashboard cards'
  artifacts:
    - path: 'components/dashboard/TransactionsPreviewTable.tsx'
      provides: 'TransactionsPreviewTable with CardTitle inheriting text-lg'
      contains: 'CardTitle className="text-navy'
      not_contains: 'CardTitle className="text-navy text-base'
    - path: 'components/dashboard/ParentBudgetOverview.tsx'
      provides: 'ParentBudgetOverview with CardTitle inheriting text-lg'
      contains: 'CardTitle className="text-base font-semibold text-navy'
      not_contains: 'text-base'
    - path: 'components/dashboard/TransparencyCard.tsx'
      provides: 'TransparencyCard with CardTitle inheriting text-lg'
      contains: 'CardTitle className="text-base font-semibold text-navy'
      not_contains: 'text-base'
  key_links:
    - from: 'components/ui/card.tsx CardTitle'
      to: 'dashboard components'
      via: 'text-lg inheritance without overrides'
      pattern: 'text-lg'
---

<objective>
Remove remaining text-base overrides from 4 dashboard components to achieve consistent 18px CardTitle rendering.

Purpose: Close typography hierarchy gaps identified in verification - 4 dashboard components still override CardTitle with text-base (16px) instead of inheriting text-lg (18px) from base component.

Output: All dashboard CardTitle elements render at consistent 18px, completing the QuickBooks typography hierarchy implementation across the dashboard.
</objective>

<execution_context>
@./.claude/get-shit-done/workflows/execute-plan.md
@./.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

# Verification context (gaps identified)

@.planning/phases/05-design-foundation/05-VERIFICATION.md

# Completed work from 05-01

@.planning/phases/05-design-foundation/05-01-PLAN.md

# Files to fix

@components/dashboard/TransactionsPreviewTable.tsx
@components/dashboard/ParentBudgetOverview.tsx
@components/dashboard/TransparencyCard.tsx
</context>

<tasks>

<task type="auto">
  <name>Remove text-base overrides from dashboard CardTitle elements</name>
  <files>
components/dashboard/TransactionsPreviewTable.tsx
components/dashboard/ParentBudgetOverview.tsx
components/dashboard/TransparencyCard.tsx
  </files>
  <action>
Remove text-base from CardTitle className attributes in 4 dashboard components (4 total instances):

**1. components/dashboard/TransactionsPreviewTable.tsx (2 instances):**

Line 112 (loading skeleton CardTitle):

- Current: `className="text-navy text-base font-semibold"`
- Change to: `className="text-navy font-semibold"`
- Remove: text-base

Line 130 (main CardTitle):

- Current: `className="text-navy text-base font-semibold"`
- Change to: `className="text-navy font-semibold"`
- Remove: text-base

**2. components/dashboard/ParentBudgetOverview.tsx (1 instance):**

Line 67 (CardTitle):

- Current: `className="text-base font-semibold text-navy"`
- Change to: `className="font-semibold text-navy"`
- Remove: text-base

**3. components/dashboard/TransparencyCard.tsx (1 instance):**

Line 21 (CardTitle):

- Current: `className="text-base font-semibold text-navy flex items-center gap-2"`
- Change to: `className="font-semibold text-navy flex items-center gap-2"`
- Remove: text-base

**Why these changes:**

Plan 05-01 established CardTitle text-lg (18px) as the default in the base component (components/ui/card.tsx). These 4 components still override with text-base (16px), breaking the consistent typography hierarchy.

Removing text-base allows CardTitle to inherit text-lg from the base component, achieving consistent 18px heading size across all dashboard cards.

**What NOT to change:**

- DO NOT change font-semibold, text-navy, flex, items-center, gap-2 - keep all other classes
- DO NOT change any other text-\* classes in these files (text-sm, text-xs are correct)
- DO NOT modify any other components beyond these 3 files
- DO NOT change CardTitle component definition (already fixed in 05-01)
  </action>
  <verify>
  Check text-base removed from CardTitle elements:

```bash
# Should find NO CardTitle with text-base in these files
! grep "CardTitle.*text-base" components/dashboard/TransactionsPreviewTable.tsx
! grep "CardTitle.*text-base" components/dashboard/ParentBudgetOverview.tsx
! grep "CardTitle.*text-base" components/dashboard/TransparencyCard.tsx
```

Verify CardTitle still has other classes (text-navy, font-semibold):

```bash
# Should find CardTitle with text-navy
grep "CardTitle.*text-navy" components/dashboard/TransactionsPreviewTable.tsx
grep "CardTitle.*text-navy" components/dashboard/ParentBudgetOverview.tsx
grep "CardTitle.*text-navy" components/dashboard/TransparencyCard.tsx
```

Count total changes:

```bash
# Should find 4 total CardTitle elements across 3 files (no text-base)
grep -c "CardTitle" components/dashboard/TransactionsPreviewTable.tsx  # Should be 2
grep -c "CardTitle" components/dashboard/ParentBudgetOverview.tsx      # Should be 1
grep -c "CardTitle" components/dashboard/TransparencyCard.tsx          # Should be 1
```

  </verify>
  <done>
text-base removed from 4 CardTitle instances:
- TransactionsPreviewTable.tsx: 2 instances (lines 112, 130)
- ParentBudgetOverview.tsx: 1 instance (line 67)
- TransparencyCard.tsx: 1 instance (line 21)

All CardTitle elements now inherit text-lg (18px) from base component. Other classes (text-navy, font-semibold, flex, gap-2) preserved.
</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <what-built>
Removed text-base overrides from 4 dashboard CardTitle elements to achieve consistent 18px rendering across all dashboard cards.

Changes made:

- TransactionsPreviewTable: 2 CardTitle instances now inherit text-lg (was text-base)
- ParentBudgetOverview: 1 CardTitle instance now inherits text-lg (was text-base)
- TransparencyCard: 1 CardTitle instance now inherits text-lg (was text-base)

Expected result: All dashboard card headings now consistently render at 18px (text-lg), completing the QuickBooks typography hierarchy.
</what-built>
<how-to-verify>
Visual verification steps:

1. **Start dev server:**

   ```bash
   npm run dev
   ```

2. **Navigate to dashboard:**
   - Visit http://localhost:3000/dashboard
   - Log in with treasurer account (full dashboard view)

3. **Verify CardTitle consistency (CRITICAL):**

   Inspect 18px rendering on fixed components using browser DevTools (F12):

   **a) Recent Transactions card (TransactionsPreviewTable):**
   - Find "Recent Transactions" CardTitle
   - Right-click > Inspect
   - Check computed font-size: Should be **18px** (was 16px before fix)
   - Verify: font-size: 18px (from text-lg class)

   **b) Budget Tracking card (ParentBudgetOverview):**
   - Find "Budget Tracking" CardTitle
   - Right-click > Inspect
   - Check computed font-size: Should be **18px** (was 16px before fix)
   - Verify: font-size: 18px (from text-lg class)

   **c) Transparency & Help card (TransparencyCard):**
   - Find "Transparency & Help" CardTitle
   - Right-click > Inspect
   - Check computed font-size: Should be **18px** (was 16px before fix)
   - Verify: font-size: 18px (from text-lg class)

4. **Compare to already-fixed cards:**

   **Budget Performance card (fixed in 05-01):**
   - Inspect "Budget Performance" CardTitle
   - Should also be 18px
   - Compare visually to newly-fixed cards - should look same size

   **Quick Actions card (fixed in 05-01):**
   - Inspect "Quick Actions" CardTitle
   - Should also be 18px
   - Compare visually to newly-fixed cards - should look same size

5. **Verify visual consistency:**
   - Unfocus eyes and scan dashboard
   - All card headings should appear SAME SIZE now (18px)
   - No jarring size differences between card titles
   - Headings should feel slightly larger/more prominent than before (upgrade from 16px to 18px)

6. **Verify typography hierarchy (overall):**
   - Metrics (30px) - largest, draws eye first
   - Card headings (18px) - medium, clearly distinct from labels
   - Labels (14px) - smaller, secondary text
   - Badges (12px) - smallest, compact

   Should create clear visual hierarchy with metrics > headings > labels > badges.

7. **Check for regressions:**
   - No layout breaks or text overflow
   - No wrapping issues on card titles
   - Icons still aligned correctly (TransparencyCard has icon in CardTitle)
   - All cards render cleanly without visual glitches

8. **Mobile responsive check:**
   - Resize browser to 320px width
   - CardTitle elements remain readable
   - No text overflow or wrapping problems
   - Visual hierarchy still clear on small screens

9. **Verify specific cards with context:**

   **Recent Transactions card:**
   - "Recent Transactions" heading at 18px
   - "Latest financial activity" subtitle at 14px (text-sm)
   - Clear size distinction between heading and subtitle

   **Budget Tracking card:**
   - "Budget Tracking" heading at 18px
   - "Season spending vs. budget" subtitle at 14px (text-sm)
   - Clear hierarchy between heading and description

   **Transparency & Help card:**
   - "Transparency & Help" heading at 18px
   - Info icon still aligned properly with text
   - Heading feels appropriately sized for card content

**Success indicators:**

- ✓ DevTools shows font-size: 18px on all inspected CardTitle elements
- ✓ All dashboard card headings appear visually consistent (same size)
- ✓ Card headings upgraded from 16px to 18px - slightly larger, more prominent
- ✓ Clear visual hierarchy: metrics > headings > labels > badges
- ✓ No layout breaks, overflow, or alignment issues
- ✓ Icons in CardTitle (TransparencyCard) still aligned correctly
- ✓ Mobile view remains clean and readable

**Failure indicators:**

- ✗ DevTools shows font-size other than 18px on any CardTitle
- ✗ Card headings appear different sizes (inconsistent)
- ✗ Text overflow or wrapping issues
- ✗ Layout breaks or misalignment
- ✗ Icons misaligned with text
- ✗ Mobile view has readability problems
  </how-to-verify>
  <resume-signal>
  Type "approved" if all CardTitle elements render at consistent 18px, no layout breaks, and visual hierarchy is clear.

If issues found, describe:

- Which card/component has the issue
- What's wrong (wrong font size, layout break, overflow, etc.)
- DevTools computed font-size value (if size issue)
- Screenshot if layout is broken
  </resume-signal>
  </task>

</tasks>

<verification>
After all tasks complete and visual verification approved:

**CardTitle text-base removal check:**

```bash
# Verify NO text-base on CardTitle in fixed files
! grep "CardTitle.*text-base" components/dashboard/TransactionsPreviewTable.tsx
! grep "CardTitle.*text-base" components/dashboard/ParentBudgetOverview.tsx
! grep "CardTitle.*text-base" components/dashboard/TransparencyCard.tsx
```

**CardTitle inheritance check:**

```bash
# Verify CardTitle still has proper classes (text-navy, font-semibold)
grep "CardTitle.*text-navy" components/dashboard/TransactionsPreviewTable.tsx
grep "CardTitle.*text-navy" components/dashboard/ParentBudgetOverview.tsx
grep "CardTitle.*text-navy" components/dashboard/TransparencyCard.tsx
```

**Base component check:**

```bash
# Verify CardTitle base still has text-lg default (from 05-01)
grep "text-lg" components/ui/card.tsx | grep CardTitle
```

**Visual verification:**

- Dashboard loads without errors
- All CardTitle elements render at 18px (DevTools inspection)
- Visual consistency across all dashboard card headings
- No layout breaks, overflow, or alignment issues
- Typography hierarchy clear: metrics (30px) > headings (18px) > labels (14px) > badges (12px)
- Icons in CardTitle aligned correctly (TransparencyCard)
- Mobile responsive behavior maintained
  </verification>

<success_criteria>
Gap closure complete when:

1. **text-base overrides removed:**
   - TransactionsPreviewTable.tsx: 2 CardTitle instances (lines 112, 130) - text-base removed
   - ParentBudgetOverview.tsx: 1 CardTitle instance (line 67) - text-base removed
   - TransparencyCard.tsx: 1 CardTitle instance (line 21) - text-base removed
   - Total: 4 instances fixed

2. **Consistent 18px rendering:**
   - All dashboard CardTitle elements render at 18px (text-lg)
   - DevTools inspection confirms font-size: 18px on all CardTitle elements
   - No CardTitle overrides with text-base remain in dashboard components

3. **Visual hierarchy complete:**
   - Card headings visually consistent across dashboard (same size)
   - Clear distinction between metrics (30px), headings (18px), labels (14px), badges (12px)
   - No jarring size differences between card titles

4. **No regressions:**
   - Dashboard loads without errors
   - No layout breaks or text overflow
   - Icons aligned correctly (TransparencyCard has icon in CardTitle)
   - Mobile responsive behavior maintained
   - All existing functionality works

5. **Gap closed:**
   - Verification gap "CardTitle text-lg default works correctly" - RESOLVED
   - All dashboard components now inherit text-lg from base component
   - Consistent QuickBooks typography hierarchy achieved across dashboard

6. **Files updated:**
   - components/dashboard/TransactionsPreviewTable.tsx (2 changes)
   - components/dashboard/ParentBudgetOverview.tsx (1 change)
   - components/dashboard/TransparencyCard.tsx (1 change)

7. **Human verification approved:**
   - Treasurer viewed dashboard and confirmed consistent 18px rendering
   - No visual issues or layout problems reported
   - Typography hierarchy clear and professional
     </success_criteria>

<output>
After completion, create `.planning/phases/05-design-foundation/05-02-SUMMARY.md` documenting:
- Gap closure results (4 text-base overrides removed)
- CardTitle consistency achieved (all dashboard cards 18px)
- Visual verification results
- Before/after comparison (16px → 18px for fixed cards)
- Remaining work (if any text-base overrides found in non-dashboard components)
</output>
